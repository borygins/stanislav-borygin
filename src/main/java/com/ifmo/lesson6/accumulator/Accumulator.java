package com.ifmo.lesson6.accumulator;

public class Accumulator interface Operation {
    double value;
    Accumulator acc = new Accumulator()
       double acc = accumulate(value);
    public static void Operatin();



}
